apiVersion: v1
kind: Service
metadata:
  name: streaming-mysql-service
spec:
  type: LoadBalancer
  selector:
    app: streaming-mysql-label
  ports:
  - port: 3306
    targetPort: 4201
    protocol: TCP
    nodePort: 32001
